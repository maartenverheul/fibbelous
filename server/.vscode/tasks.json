{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build server",
      "type": "shell",
      "command": "cargo",
      "args": ["build", "--bin", "server"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$rustc"]
    }
  ]
}
